---
- name: Frequently used OS tools
  hosts: pool0
  remote_user: root

tasks:
  - name: Check if net-tools is latest
    ansible.builtin.apt:
      name: net-tools
      state: latest
 
  - name: Check if git is latest
    ansible.builtin.apt:
      name: git
      state: latest
